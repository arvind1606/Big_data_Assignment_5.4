import numpy as np

lst = [3, 5, 7, 2, 8, 10, 11, 65, 72, 81, 99, 100, 150]
arr = np.array(lst)

window_sz = 3
count_moving_elements = len(arr) - window_sz + 1

out = []
for i in range(count_moving_elements):
    (out.append(sum(arr[i:i+window_sz])))
    
print(out)
